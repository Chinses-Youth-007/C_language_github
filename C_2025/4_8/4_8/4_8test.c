#define  _CRT_SECURE_NO_WARNINGS  
#include <stdio.h>


int main()
{
	//	char ch = 0;
	//	scanf("%c", &ch);
	//	printf("%c-----\n",ch);
	//	//char类型遇到空格会识别到空格，从而代替输入的字符
	//
	//	int c = 0;
	//	scanf("%d", &c);
	//	printf("%d", c);
	//	//scanf会跳过空格，直至\0出现停止识别

	/*char data[4] = { 0 };
	scanf("%4s", data);
	printf("%4s", data);*/

	//char num[2] = { 0 };
	//scanf("%[^\n]s", num);//【[^\n]，读取一个字符，直到遇到\n停止
	//printf("%s\n", num);


	//int a = 0;
	//int b = 0;
	//scanf("%d%*c%d", &a, &b);//输入格式要与占位符保持一致；////*c会忽略一个字符
	//printf("%d %d", a, b);


	//if(表达式)，表达式=0，假。表达式！=0，为真。
	/*if 
	else if*/
		//else
		///多层判断

   //默认情况，一个if对应一个一个语句，如果有多条语句，需要大括号括起来。









	return 0;
}